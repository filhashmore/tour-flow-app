"use strict";

import useStableCallbackValue from './useStableCallbackValue';
export default function useStableCallbackValues(callbacks) {
  return Object.fromEntries(Object.entries(callbacks).map(([key, callback]) => [key,
  // eslint-disable-next-line react-hooks/rules-of-hooks
  useStableCallbackValue(callback)]));
}
//# sourceMappingURL=useStableCallbackValues.js.map