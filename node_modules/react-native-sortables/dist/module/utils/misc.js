"use strict";

export const isPresent = value => {
  'worklet';

  return value !== undefined && value !== null;
};
export const toPair = value => Array.isArray(value) ? value : [value, value];
//# sourceMappingURL=misc.js.map