"use strict";

export const orderItems = (data, items, {
  fromIndex,
  keyToIndex,
  toIndex
}, skipIfNoChange) => {
  if (skipIfNoChange && fromIndex === toIndex) {
    return data;
  }
  const result = [];
  for (let i = 0; i < items.length; i++) {
    result[keyToIndex[items[i][0]]] = data[i];
  }
  return result;
};
export const getKeyToIndex = itemKeys => {
  'worklet';

  return itemKeys.reduce((acc, key, index) => {
    acc[key] = index;
    return acc;
  }, {});
};
//# sourceMappingURL=order.js.map