import type { Vector } from '../../../types';
type DebugRectColors = {
    backgroundColor: string;
    borderColor: string;
};
declare const DEBUG_RECT_KEYS: readonly ["bottom", "left", "right", "top"];
type DebugBox = Record<(typeof DEBUG_RECT_KEYS)[number], {
    hide: () => void;
    update: (from: Vector, to: Vector, colors?: DebugRectColors) => void;
}> & {
    hide: () => void;
};
export default function useDebugBoundingBox(isAbsolute?: boolean): DebugBox | undefined;
export {};
//# sourceMappingURL=useDebugBoundingBox.d.ts.map