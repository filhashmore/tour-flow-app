import type { ReactNode } from 'react';
import type { RenderItem } from '../../../types';
export type Store<I> = {
    getKeys: () => Array<string>;
    subscribeKeys: (listener: () => void) => () => void;
    getNode: (key: string) => ReactNode | undefined;
    subscribeItem: (key: string, listener: () => void) => () => void;
    update: (entries: Array<[key: string, item: I]>, renderItem?: RenderItem<I>) => void;
};
export declare function createItemsStore<I>(initialItems?: Array<[string, I]>, initialRenderItem?: RenderItem<I>): Store<I>;
//# sourceMappingURL=store.d.ts.map