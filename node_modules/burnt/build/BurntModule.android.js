import { ToastAndroid } from "react-native";
export default {
    alertAsync({ message, title, preset, duration }) {
        this.toastAsync({
            message,
            title,
            preset: preset == "done" ? "done" : preset == "error" ? "error" : "done",
            duration,
        });
    },
    toastAsync({ title, duration = 5, from = 'bottom' }) {
        ToastAndroid.showWithGravityAndOffset(title, duration * 1000, from === 'bottom' ? ToastAndroid.BOTTOM : ToastAndroid.TOP, 25, 50);
    },
    dismissAllAlertsAsync() { },
};
//# sourceMappingURL=BurntModule.android.js.map